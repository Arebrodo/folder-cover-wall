## Folder Cover Wall 0.6.2

This maintenance release fixes an automated Community Directory review error introduced in 0.6.1.

### Fixes

- Replaced a directly-created HTML heading in the plugin settings with Obsidian's `Setting.setHeading()` API.
- Keeps the v0.6.0 memory optimizations and v0.6.1 duplicate-sidebar-tab fix unchanged.

No intended user-facing behavior changes.
